namespace LunraGames.SubLight
{
	/// <summary>
	/// Value filter operations, groups them with others so all their values can
	/// be evaluated.
	/// </summary>
	public enum ValueFilterGroups
	{
		Unknown = 0,
		Any = 10,
		All = 20,
		None = 30
	}
}